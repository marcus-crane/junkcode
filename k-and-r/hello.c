/* Include the standard input/output library */
#include <stdio.h>

/* Main is a function that, in this case, takes no arguments.
   The first function always needs to be called main but beyond that, there are no proper restrictions
*/

main() {
  printf("hello, world\n");
}

/* printf prints a formatted string. In this case, it properly parses \n as being a newline character.
   If you use regular print, it doesn't seem to know what to do with \n. Not sure on specifics yet.
   You also have to end all statements with a semicolon (;)
/*
